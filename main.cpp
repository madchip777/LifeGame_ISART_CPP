// This serve as the "head" of project, so no needs to put hands inside the /src to run / compile it.
// It also serves as an "always root" when the project change inside.

#include "src/Core/MainMenu.h"

int main() {
    return StartMenu();
}
